# Artifacts

This directory stores generated classifier models, recommender models, split
metadata, and evaluation outputs created by offline training commands. These
files are not tracked in git.

## Generated by Offline Steps

- `python main.py --step classify`
- `python main.py --step benchmark`
- `python main.py --step cluster`
